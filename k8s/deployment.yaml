apiVersion: apps/v1
kind: Deployment
metadata:
  name: garbage-classifier
  labels:
    app: garbage-classifier
spec:
  replicas: 2
  selector:
    matchLabels:
      app: garbage-classifier
  template:
    metadata:
      labels:
        app: garbage-classifier
    spec:
      containers:
      - name: garbage-classifier
        image: garbage-classifier:v1
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5